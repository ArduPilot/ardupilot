/*
 * Linkage configuration.
 */

/* region (origin, length) */
ROM (0x00001000, 8K)
/* region (origin, length) */
RAM (0x80000000, 512M)

/* section (region[, lma_region]) */
text   (RAM)
rodata (RAM)
data   (RAM)
bss    (RAM)
