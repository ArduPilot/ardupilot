# ArduPilot SITL Docker Configuration
# =====================================
# Copy this file to .env and modify as needed:
#   cp .env.example .env
#
# All parameter names are VERIFIED from ArduPilot source code.
# Reference files:
#   - Tools/autotest/sim_vehicle.py (command-line options)
#   - libraries/AP_DDS/AP_DDS_Client.cpp (DDS parameters)
#   - libraries/AP_Networking/AP_Networking_address.cpp (IP parameters)

# ==========================================
# SITL Command-Line Options (sim_vehicle.py)
# ==========================================

# Vehicle type
# Valid: ArduCopter, ArduPlane, ArduRover, ArduSub, AntennaTracker, Blimp
VEHICLE=ArduCopter

# Frame/model type (depends on vehicle)
# ArduCopter: quad, hexa, octa, tri, y6, gazebo-iris, etc.
# ArduPlane: plane, quadplane, gazebo-zephyr, etc.
# ArduRover: rover, boat, gazebo-rover, etc.
# ArduSub: vectored, gazebo-bluerov2, etc.
MODEL=quad

# Simulation speedup multiplier
# 1 = real-time, 10 = 10x faster
SPEEDUP=1

# SITL instance number
# Adds 10*instance to all port numbers
INSTANCE=0

# IP address of external simulator (Gazebo/JSBSim)
# Set to remote Gazebo host IP for external simulation
SIM_ADDRESS=127.0.0.1

# Enable DDS/ROS2 support
# 1 = enabled, 0 = disabled
ENABLE_DDS=1

# Wipe EEPROM parameters on startup
# 1 = wipe, 0 = keep existing
WIPE=0

# Use synthetic clock (for faster-than-realtime simulation)
# 1 = synthetic, 0 = real clock
SYNTHETIC_CLOCK=0

# Number of JSON slave instances
SLAVE=0

# MAVLink system ID (leave empty for default)
# SYSID=1

# Home location: lat,lon,alt,heading or location name
# Example: "35.363261,149.165230,584,353"
# HOME_LOCATION=

# Path to additional parameter file inside container
# CUSTOM_DEFAULTS=/ardupilot/my_params.parm

# ==========================================
# ArduPilot Build Options
# ==========================================

# Skip build step (use pre-built binary)
# 1 = skip, 0 = build
SKIP_BUILD=0

# Build target board
BUILD_TARGET=sitl

# Additional waf configure options
# Example: "--debug --enable-math-check-indexes"
# WAF_CONFIGURE_OPTS=

# Number of parallel build jobs (leave empty for auto)
# BUILD_JOBS=

# ==========================================
# DDS/ROS2 Parameters
# Source: libraries/AP_DDS/AP_DDS_Client.cpp
# ==========================================
# These are ACTUAL ArduPilot parameters that will be
# written to the parameter file and loaded at startup.

# DDS_ENABLE: Enable DDS subsystem
# Values: 0=Disabled, 1=Enabled
# Requires reboot after change
DDS_ENABLE=1

# DDS_UDP_PORT: UDP port for DDS/Micro ROS Agent
# Range: 1-65535
# Default: 2019
DDS_UDP_PORT=2019

# DDS_IP: IP address of Micro ROS Agent
# The agent runs on the ROS2 host, ArduPilot connects to it
# Format: Each octet is a separate parameter
# Example: 192.168.1.100 = IP0=192, IP1=168, IP2=1, IP3=100
DDS_IP0=127
DDS_IP1=0
DDS_IP2=0
DDS_IP3=1

# DDS_DOMAIN_ID: ROS_DOMAIN_ID for DDS
# Range: 0-232
# Must match the ROS2 domain on the agent side
DDS_DOMAIN_ID=0

# DDS_TIMEOUT_MS: Time to wait for XRCE agent response
# Range: 1-10000 ms
# Default: 1000
DDS_TIMEOUT_MS=1000

# DDS_MAX_RETRY: Max ping attempts before giving up
# Range: 0-100
# 0 = unlimited retries
DDS_MAX_RETRY=10

# ==========================================
# Serial DDS Configuration (Optional)
# Only needed if using serial instead of UDP
# ==========================================

# SERIAL1_PROTOCOL: Set to 45 for DDS on serial port
# 45 = SerialProtocol_DDS_XRCE
# SERIAL1_PROTOCOL=45

# SERIAL1_BAUD: Baud rate for serial DDS
# 115 = 115200 baud
# SERIAL1_BAUD=115

# ==========================================
# MAVProxy Options
# ==========================================

# Enable MAVProxy GCS
# 1 = enabled, 0 = disabled (no-mavproxy mode)
MAVPROXY_ENABLED=0

# MAVProxy output destination
MAVPROXY_OUT=127.0.0.1:14550

# MAVProxy master connection
MAVPROXY_MASTER=tcp:127.0.0.1:5760

# MAVProxy SITL port
MAVPROXY_SITL=127.0.0.1:5501
